server:
  http_listen_port: 9080

positions:
  filename: /tmp/positions.yaml

clients:
  - url: http://loki:3100/loki/api/v1/push
    reject_old_entries: true

scrape_configs:
  - job_name: docker

    docker_sd_configs:
      - host: unix:///var/run/docker.sock
        refresh_interval: 5s
        filters:
          - name: auth-service
          - name: users-service
          - name: products-service
          - name: orders-service
          - name: payment-service
          - name: notification-service


    pipeline_stages:
      - docker: {}

      - json:
          expressions:
            service: service
            level: level
            traceId: traceId
            spanId: spanId
            method: req.method
            url: req.url

      - timestamp:
          source: timestampMs
          format: UnixMilli

      - labels:
          service:
          level:
          method:
          traceId:

      - static_labels:
          job: docker

    relabel_configs:
      - source_labels: ["__meta_docker_container_name"]
        regex: "/(users-service|auth-service|products-service|orders-service|payments-service|notifications-service)"
        action: keep

      - source_labels: ["__meta_docker_container_name"]
        regex: "/(.*)"
        target_label: "container"

      - source_labels: ["container"]
        target_label: "job"

# DEV - LOG
# scrape_configs:
#   - job_name: docker

#     pipeline_stages:
#       - docker: {}

#       - json:
#           expressions:
#             service: service
#             level: level
#             traceId: traceId
#             spanId: spanId
#             method: req.method
#             url: req.url

#       - labels:
#           service:
#           level:
#           method:
#           status:
#           traceId:

#     static_configs:
#       - targets:
#           - localhost
#         labels:
#           job: docker
#           __path__: /var/lib/docker/containers/*/*.log

